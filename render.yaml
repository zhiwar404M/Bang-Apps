services:
  - type: web
    name: islamic-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/server.py"
    autoDeploy: true
    plan: free
    envVars:
      - key: PORT
        value: 8001
    healthCheckPath: "/docs"
